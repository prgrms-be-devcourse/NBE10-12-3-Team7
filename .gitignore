# Gradle
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar

# IntelliJ 자체 컴파일러 사용 시 출력 경로(Gradle 위임 빌드가 아닐 때). 소스 리소스 변경 후에도
# 재빌드 전까지 여기 있는 옛 application.yml 등이 그대로 실행돼 혼란을 유발하므로 추적하지 않는다.
bin/

# IDE
.idea/
*.iml
.vscode/
.DS_Store

# Java
*.class
*.log

# 로컬 파일 업로드 저장소 (신고 증빙 이미지 등)
uploads/

# 환경 설정 / 시크릿
application-local.yml
application-secret.yml
.env

# Claude Code / 로컬 분석 도구 (개인 절대경로 포함 — 팀 레포에 올리지 않음)
.mcp.json
.serena/
.claude/

# Kotlin 컴파일러 데몬 세션 파일(빌드 산출물)
.kotlin/
