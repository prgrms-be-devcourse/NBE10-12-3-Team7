브라우저가 반드시 필요한 시나리오만 여기 둔다.

현재 대상은 REST 로 찌를 수 없는 실시간 기능 두 개다:
  - 채팅 실시간 수신 (탭 2개)
  - 경매 입찰 — AuctionBidController 가 @MessageMapping("/auction/{id}/bid") 라 REST 엔드포인트가 없다

나머지 백엔드 검증은 전부 specs/api 로 간다. UI 스펙은 느리고 셀렉터 때문에 잘 깨지므로
"여기 아니면 검증이 불가능한가?"에 예라고 답할 수 있을 때만 추가한다.
