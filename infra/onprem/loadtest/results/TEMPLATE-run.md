# 회차 요약 — <시나리오> / <메모>

조건은 옆의 `conditions.md` 에 자동으로 남아 있다. 여기는 **그래서 무엇을 알게 됐는가**를 쓴다.

## 무엇을 확인하려고 돌렸나

<한 줄. 이게 없으면 다음 회차에 "뭘 확인하려고 돌리지?"를 처음부터 다시 정하게 된다>

## 결과

| 항목 | 값 |
|---|---|
| 이 회차 무부하 기준선 | ms (`setup()` 실측) |
| 배수 p95 | 배 ← **판정은 이 값** |
| 배수 p99 | 배 |
| 실패율 | % |
| 처리량 | req/s |
| 버린 반복 (`dropped_iterations`) | |
| 판정 | 허용선 안 / 넘음 |

## 계단별 (Grafana 시간축에서 읽는다)

k6 요약은 계단 전체의 합산값이라 무릎 위치에 답하지 못한다.

| 초당 진입 | 배수 p95 | VM CPU | 비고 |
|---|---|---|---|
| 20 | | | |
| 50 | | | |
| 100 | | | |
| 200 | | | |
| 400 | | | |
| 600 | | | |

## 무릎

<초당 몇 진입에서 꺾였나. 꺾이지 않았으면 "이 계단에서는 안 꺾임"이라고 적는다 —
그것도 결과다>

**무엇이 먼저 포화됐나:** <커넥션 / CPU / buffer pool / 알 수 없음>
**근거:** <Grafana 어느 행의 무엇을 보고 그렇게 판단했나>

## 버린 구간

<`dropped_iterations` 가 0 이 아니거나 VM CPU 가 90% 를 넘은 구간. 없으면 "없음">

이 구간은 앱이 아니라 부하 생성기·호스트를 잰 것이라 판정에 쓰지 않는다.

## 다음 회차의 목적

<이 회차가 남긴 질문. FINDINGS 식으로 다음에 무엇을 확인할지 한 줄>
